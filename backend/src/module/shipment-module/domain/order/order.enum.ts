export enum OrderPaymentStatusEnum {
    PENDING = 'pending',
    PAID = 'paid',
    CANCELLED = 'cancelled',
    REFUND = 'refund'
}

export enum OrderStatusEnum {
    PENDING = 'pending',
    PLACED = 'placed',
    BILLED = 'billed',
    DELIVERED = 'delivered',
    RETURNED = 'returned'
}