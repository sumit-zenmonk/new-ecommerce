export enum OrderPaymentStatusEnum {
    PENDING = 'pending',
    PAID = 'paid',
    CANCELLED = 'cancelled',
    REFUND = 'refund'
}

export enum OrderStatusEnum {
    PENDING = 'pending',
    PROCESSING = 'processing',
    PACKED = 'packed',
    DELIVERED = 'delivered',
    RETURNED = 'returned'
}